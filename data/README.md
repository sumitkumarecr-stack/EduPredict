# data/

- `sample_students.csv` – 25 **synthetic**, input-only rows for trying Batch Predictions (committed).
- `students_synthetic.csv` – full synthetic training set, created by `python scripts/generate_demo_data.py` (git-ignored, reproducible).

Never place real student records here in a public repository.
